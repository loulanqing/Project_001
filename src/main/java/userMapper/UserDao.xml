<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="userMapper.UserDao">
    <select id="selectBlog" resultType="userMapper.Test">
        select * from test where te01 = #{id}
    </select>
    <insert id="insert" parameterType="userMapper.Test" >
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        insert into TEST (TE00, TE01, TE02,
        TE03)
        values (#{te00,jdbcType=NUMERIC}, #{te01,jdbcType=VARCHAR}, #{te02,jdbcType=VARCHAR},
        #{te03,jdbcType=VARCHAR})
    </insert>
</mapper>